import { DotMatrixLoader } from "@/components/DotMatrixLoader";

export default function Loading() {
  return <DotMatrixLoader className="min-h-[calc(100dvh-3.5rem)]" label="Loading generation" />;
}
