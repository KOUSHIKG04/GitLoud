{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./client": "./src/client.ts"
  },
  "scripts": {
    "build": "npm run db:generate && tsc && tsc-alias -p tsconfig.json --resolve-full-paths",
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "db:push": "prisma db push --schema=./prisma/schema.prisma",
    "db:studio": "prisma studio --config=./prisma.config.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.0.0",
    "@prisma/client": "^7.0.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "prisma": "^7.0.0",
    "tsc-alias": "^1.8.16",
    "typescript": "5.9.2"
  }
}
